import { Typography } from "@mui/material"
import Layout from "../../components/layout"

export default ({})=>{
    return <Layout>
        <Typography variant="body1">Simple Marketplace</Typography>
    </Layout>
}